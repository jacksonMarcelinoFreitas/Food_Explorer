import { styled } from "styled-components";

export const Container = styled.div`
  padding: 0 24px;

  .title-area{
    font-family: 'Poppins' sans-serif;
    font-size: 18px;
    font-weight: 500;

    margin-bottom: 24px;
  }
`